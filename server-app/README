This is the backend for the ios-clickstart application

To run this locally, set the following environment variables to connect to a couchDB server:

cloudant_server_address
cloudant_username
cloudant_password
couchdb_name (the name you want it to use in the DB)

run locally in dev mode: 
play run

deploy:

play dist
bees app:deploy -type play2 -a yourAppId dist/app name.zip




